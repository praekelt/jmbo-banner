sudo: false
language: python
cache: pip

env:
  - TOXENV=django111-py27
  - TOXENV=django111-py35
  - TOXENV=django111-py36

install:
  - pip install tox
  - pip install coveralls

script:
  - tox

after_success:
  - coveralls
